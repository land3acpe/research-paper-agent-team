schedule:
  enabled: false
  mode: weekly
  day: sunday
  time: "09:00"
  timezone: "Asia/Shanghai"

window:
  daily_days: 3
  weekly_days: 14
  monthly_days: 45

limits:
  max_candidates_per_source: 100
  max_total_candidates: 300
  max_runtime_minutes: 30
